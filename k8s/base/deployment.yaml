apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-librarian
  labels:
    app.kubernetes.io/name: backend-librarian
spec:
  # replicas 는 overlay 에서 환경별로 지정 (dev=1, prod=2 — prod 는 현재 주석 상태)
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: backend-librarian
  template:
    metadata:
      labels:
        app.kubernetes.io/name: backend-librarian
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: backend-librarian
          # 이미지 실제 경로/태그는 overlay 의 images: 트랜스포머가 주입 (CI 가 태그 갱신)
          image: backend-librarian
          # develop-latest 처럼 고정 태그를 재사용하므로, 노드에 캐시된
          # 옛 이미지를 쓰지 않도록 항상 새로 pull 한다.
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: backend-librarian-config
            - secretRef:
                name: backend-librarian-secret
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          # boto3/strands 등이 임시 파일을 쓸 수 있어 쓰기 가능한 /tmp 를 제공
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
